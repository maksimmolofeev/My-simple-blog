
export const selectIsAuth = state => state.userSlice._isAuth;

export const selectUser = state => state.userSlice._user;

export const selectCollection = state => state.productSlice._collection;

export const selectProduct = state => state.productSlice._product;